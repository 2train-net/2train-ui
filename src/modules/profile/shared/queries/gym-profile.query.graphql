query gymProfile($where: UserWhereUniqueInput!) {
  user(where: $where) {
    gym {
      uuid
      name
      branches {
        uuid
        address {
          country
        }
      }
    }
  }
}
