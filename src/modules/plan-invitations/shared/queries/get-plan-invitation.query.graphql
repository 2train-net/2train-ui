query getPlanInvitation($where: PlanInvitationWhereUniqueInput!) {
  payload: planInvitation(where: $where) {
    link
    plan {
      name
      price
      currency
      intervalCount
      intervalPlan
    }
  }
}
